{
  "name": "three-ntc-trainer",
  "version": "0.0.0",
  "description": "GPU trainer that bakes a MaterialX material into a .ntc Neural Texture Compression model.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "dev": "tsc -w --preserveWatchOutput --noEmitOnError",
    "test": "vitest run"
  },
  "dependencies": {
    "fflate": "^0.8.2",
    "three-ntc": "workspace:*"
  },
  "peerDependencies": {
    "three": ">=0.180.0"
  },
  "devDependencies": {
    "three": "^0.186.0"
  }
}
